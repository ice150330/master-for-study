# CHANGES.md · 变更记录

> 倒序记录，每次修改后追加一条。

## 2026-08-25

- 全局手绘字体与图标按钮缩减：接入**站酷快乐体**（ZCOOL KuaiLe，GitHub/Google Fonts 开源可商用、圆润手写风近似奶酪体）作为全局 body 字体（`@fontsource/zcool-kuaile` 按 unicode-range 93 分片按需加载，缺字回退 Geist/系统，代码块保持等宽），此前试行的霞鹜文楷（楷体风）按验收反馈移除；图标即可表意的按钮缩减为纯图标 + Tooltip 悬停显示功能——发送/停止（44px 方形图标钮）、引用资源（书本图标 + 已选数角标）、会话树抽屉的新话题（头部 + 图标，保留关闭钮），文字承载语义的回复操作行与对话框确认保持文字。无头浏览器验证：body 计算字体 ZCOOL KuaiLe 生效、中文字形已加载、发送钮 44×44 纯图标、Tooltip 悬停出现
- 会话界面第二轮回排 + 主题与渲染升级（验收反馈六项）：**工作区切换器**移出侧边栏底部、改为会话卡头独有（侧栏工具区仅剩搜索 / 设置 / 主题）；**全局主题扩为四种内置纸张**（纸白 / 暖纸 / 青蓝 / 夜墨），真相源迁至 `<html data-theme>`（防闪脚本与 theme-client 同步迁移，旧值 light/dark 自动映射，`dark:` 变体重定义为仅夜墨），主题切换升级为调色板弹层四选一（侧栏与设置弹窗双入口），夜墨为唯一深色主题（墨绿底 / 米色墨 / marker 提亮全套令牌）；**分支胶带改堆叠**——多条子分支在下缘右下角逐条向左叠压 46px 只露图标与层号，悬停浮到最上层展开完整标题；**会话卡头压成单行**（D 徽标 · 标题 · 血缘与计数 · 工具组）；**全局会话 Markdown 渲染**（react-markdown + remark-gfm，新增 `.md-chat` 手绘主题块覆盖标题 / 列表 / 引用 / 代码 / 表格 / 链接，`[[术语]]` 在文本节点内解析高亮、行内代码保持字面，用户消息仍纯文本防误格式化）；**回复下方常驻操作行**（重新生成——仅最后一条可原位重写，详细综述——从该回答派生分支生成结构化综述，从这里分支），移除输入工具行的重新生成 / 继续回答与用户消息旁的分支按钮。无头浏览器验证：四主题切换与刷新持久、mock 三分支堆叠间距 46px / z-index 递减 / 悬停置顶 / 点击露条切换、MD 四类元素渲染、卡头单行、操作行三按钮，全绿；旧 ui-foundations 的 `.dark` 截图用例同步改为 `data-theme=night`
- 重组全局壳与会话页布局（验收反馈）：**全局壳**移除 52px 顶栏（页面位置信息并入底部上下文条），工作区切换器与搜索 / 设置 / 主题下沉到侧边栏底部工具区（≥1180 全宽行 / 更窄收成图标），工作台设置由 22rem 弹层升级为 38rem 大弹窗（标题 + 分区滚动，侧边栏与移动端共用一个实例）；移动端底部导航加第五格「工具」直达设置。**会话页**删除卡外工具行——会话树（带会话数角标）与新话题收进卡片头部图标组，老师风格、闪电/深思合并后的单按钮双态切换、引用资源、重新生成 / 继续回答全部收进输入框上方的 h-7 紧凑工具条，输入框与发送按钮统一 44px 同高（`field-sizing-content` 随内容增高至 160px）；纸签改为**骑缝倾斜胶带**——父级链贴主卡上缘从左上向右排（黄色），分支贴下缘从右下向左排（青色），胶带一半在卡外页面留白、不遮挡卡内内容，桌面移动统一。**概念详情**由右侧轨道改为便利贴弹窗（居中 30rem、黄便签纸底 + 顶部青胶带，Radix 焦点语义，定义 / 来源 / 相关 / 动作全保留）。**学习上下文条**移到页面最底部并改为常驻（h-8 紧凑条：工作区 → 来源 → 概念 → 作答 → 当前位置；无上下文时显示工作区与当前位置），移动端位于底部导航之上。无头浏览器冒烟（桌面 1440 + 移动 390）全部断言通过（顶栏消失 / 侧栏工具 / 弹窗尺寸 / 输入同高 / 胶带渲染 / 无横向溢出）；DESIGN.md §2 §6 同步改写。已知遗留：旧 `concept-rail` / `accessibility-layout` 等 E2E 用例仍按右侧轨道与顶栏选择器编写，待回归更新
- 修复术语黑名单快照引发的消息区无限重渲染：`readTermBlacklist()` 每次调用都 `new Set()`，`useSyncExternalStore` 的 getSnapshot 引用不稳定，React 视为持续变更刷出 "Maximum update depth exceeded"（有消息的聊天页必崩）；改为按 localStorage 原始字符串缓存解析结果，相同内容复用同一 Set 引用、变更才换新引用，返回类型收紧为 `ReadonlySet<string>`。全站其余四处 store（主题 / 模型 / 风格 / 稍后处理）均返回原始值，无同类问题；无头浏览器实测消息区 6 条消息渲染、控制台零错误
- 重设计会话卡片堆叠交互为「边缘纸签 + 会话树抽屉」：删除实体卡堆叠（AncestorStack / BranchFan）与主卡四周的永久预留（原上下各 ~112px 空间税归零），父级改为贴主卡左缘的手绘索引纸签、子分支贴右缘，越上游越向外探出形成深度暗示，每侧 3 枚直接可见、溢出收进「+N」纸签；新增右侧滑入的会话树抽屉（Radix Dialog 派生，焦点陷阱 / Escape / 焦点返回），吸收原 SessionPicker 的搜索、置顶标记、归档恢复与新话题，树视图复用 `buildSessionTree` 与白板同源并补相对时间；桌面与移动统一走纸签 + 抽屉（移除 <720px 面包屑双实现）；DESIGN.md §6 同步改写。冒烟验证（Playwright）：抽屉开合 / Escape / 搜索 / 新话题、多级会话纸签渲染与点击切换均通过；旧 `semantic-branch` 等 E2E 用例仍指向已删除的堆叠选择器，待后续回归更新

- 使用回路优化方向文档更新进度：P2 批七项（B6/B4/C3/C4/C2/B3/B5）全部交付，P0/P1/P2 三批次完结，仅多岗位能力树种子留待后续内容建设
- 落地概念关系复习卡（优化方向 B5）：`review_cards` 加 `variant/question/answer` 列（迁移 0020），`setReviewCardVariant()` 从知识图挑选两端都已学到概念的最强边，把「X 与 Y 是什么关系」问答快照到卡片（边重建不影响已发卡），切换不重置 FSRS 状态、不写日志；复习页关系卡以问题为正面、关系与两边定义为答案，侧栏提供「换成关系卡 / 换回定义卡」，无可用关系时给出可行动提示；关系卡照常参与 FSRS 评级与队列治理
- 落地成长目标驱动知识图主线（优化方向 B3 起步）：`lib/knowledge/goal.ts` 目标关键词展开（岗位同义词域 + 自定义目标 2 字滑片兜底），`getKnowledgeGraph` 按设置的成长目标为命中节点打 `mainline` 标（服务端计算，清除目标即失效）；白板知识卡主线节点青色描边 +「主线」徽标，工具栏显示「目标主线 · <目标>」；多岗位能力树种子留待后续内容建设
- 改善冷启动（优化方向 C2）：`lib/chat-starters.ts` 按成长目标定制三枚起步问题（后端 / 前端 / 算法·AI / 转行求职 / 在校学生各有专属，未识别目标回退为围绕目标的三问，无目标给通用起步），聊天空会话状态渲染为一键发送的引导 chips，降低第一次提问门槛
- 落地工作区归档与删除（优化方向 C4）：`workspaces` 加 `archived_at`（迁移 0019），归档后不再出现在切换列表、可随时恢复；删除在事务内按依赖序清理本区全部过程数据（会话/消息/笔记/资源/面试/复习/知识图/事件），概念库为全局单源保留；当前工作区归档与删除均被拒绝（409）；工作区切换器新增「管理工作区」对话框（归档/恢复/删除，删除带二次确认与导出提示）
- 落地成本感知（优化方向 C3）：`messages` 加 `usage_input/output/total` 列（迁移 0018），`/api/chat` 在 onFinish 捕获 AI SDK usage 一并落库（历史消息为空不计入）；`GET /api/usage` 输出今日 / 累计摘要，设置面板底部展示；聊天失败信息按错误特征翻译为可行动提示——限流（429）→稍后再试、余额不足→充值、密钥无效→检查 `.env`、网络失败→检查连接
- 落地隐性感知起步（优化方向 B4，蓝图 2.1「从行为推断记忆强度」）：`lib/analytics/weakness.ts` 薄弱度评分——FSRS 难度为主 + 状态加权（relearning+2/learning+1）+ 回忆偏慢加权（超过全局中位 1.2 倍起计、每多 40% 加 1 分、封顶 3，缺数据不加权）；已用于记忆画像薄弱概念排序（难度预筛 24 → 评分重排取 8）与今日页薄弱测验推荐，"答对但想得慢"从此计入薄弱证据（0 值时长日志不计入统计）
- 接线场景风格绑定（优化方向 B6）：`interviewStyleFromTeacher()` 把六型老师风格映射到面试三型（严师→严格、实战→简洁、其余→引导），面试设置默认风格按「面试场景覆盖 ?? 全局默认」自动预选（仅首次同步，不覆盖用户选择）；复习页完成语按「复习场景覆盖 ?? 全局风格」措辞（严师收束 / 陪伴鼓励 / 费曼复述各有文案）；设置面板新增「场景绑定」区，面试风格与复习语气各自可覆盖或恢复跟随全局
- 落地全局内容搜索（优化方向 C1）：`GET /api/search?q=` 跨会话标题、消息正文、概念、笔记与资源检索（当前工作区作用域，LIKE 通配符转义，每类限 5，消息命中带所属会话与关键词上下文摘要）；顶栏搜索对话框升级——页面命中之外增加「内容命中」分组，按类型徽标跳转（会话/消息定位/概念轨道/笔记/资源），250ms 防抖请求
- 落地概念定义可编辑（优化方向 B2）：`PATCH /api/concepts/[id]` 单字段修正术语定义（4–2000 字校验，404 语义），Concept 轨道定义区新增「修正定义」内联编辑（保存后重载详情）；术语是单源对象，复习卡、知识图与各处引用经动态 join 自动拿到新定义，不再被一次性 LLM 提取的错误定义锁死
- 落地整库导入恢复（优化方向 B1）：`POST /api/import` 接收导出 JSON，仓库层 `importWorkspaceData()` 事务内「先清空再插入」——`PRAGMA defer_foreign_keys` 让顺序对外键免疫，日期列按 schema 列类型从 ISO 还原为 Date，未知表键整笔拒绝不产生半状态；设置面板「数据备份」区新增导入入口，选文件后强制确认弹窗（提示先导出留底），成功后整页刷新；导出→变更→导入往返与非法结构均有集成 / 合同测试覆盖
- 落地到期提醒与逾期引导（优化方向 A4）：`/api/review?summary=1` 轻量负荷端点（摘要 + 待确认数，供轮询）；壳层挂载 `ReviewReminder`——有到期卡时页面标题显示 `(N)` 徽标（每 5 分钟刷新、不冻结路由标题），浏览器通知授权开启后应用打开期间每日至多提醒一次（localStorage 按日去重；应用未打开不产生系统通知，为本地 Web 的明确边界）；设置面板新增「复习提醒」授权入口（含拒绝/不支持状态说明）
- 落地 AI 老师记忆注入（优化方向 A1，蓝图「有记忆」承诺）：`workspace_settings` 加 `memory_injection` 开关（迁移 0017，默认开）；`getLearnerProfileSnapshot()` 聚合近期学习主题（最近会话标题）与薄弱概念（已入队、非 new、按 FSRS 难度降序、上限 8），`learnerMemoryPrompt()` 生成注入段——要求自然延续教学、禁止复述清单或宣称记忆；`/api/chat` 系统提示词按「术语标注 + 风格 + 深浅 + 记忆 + 资源」组装，画像为空或开关关闭时不占提示词，设置面板新增「老师记忆」开关
- 落地复习队列治理与标注纠错（优化方向 A2）：`term_masteries` 加 `queue_status`（迁移 0016），新概念默认「待确认」不进队列，Concept 轨道与复习页空态提供确认入队（单个/批量）；复习页新增跳过本次（+1 天）、降低频率（+30 天）与移出队列（可在概念轨道恢复），顺延不写复习日志；新增术语高亮黑名单（localStorage）——概念轨道「不再高亮」后消息区解析层自动降级为普通文本；逾期时复习页给「先清最旧」引导文案。期间修正：`terms` 表实为全局唯一（name 全表约束），概念本就跨工作区单源，A3 决策项前提有误待修订方向文档
- 沉淀下一轮使用回路优化方向（`docs/plans/2026-08-24-使用回路优化方向.md`）：复盘全回路使用短板——P0 三项（AI 记忆注入、复习队列治理与标注纠错、到期提醒）、P1 断点（导入、定义可编辑、全局搜索）、A3 概念归属产品决策项（全局化 vs 学科宇宙）及 P2/P3 实用性清单，并吸收上轮全部遗留项
- 落地多工作区（蓝图 4.1：一个工作区 = 一个学习主题）：`workspaces` 表加 `isActive`（迁移 0015），`ensureWorkspace()` 升级为「激活工作区」语义并对历史数据自愈；新增 `GET/POST /api/workspaces`（新建即切换）与 `PATCH /api/workspaces/[id]`（重命名 / 激活切换，事务保证至多一个激活）；壳层「本地工作区」徽标升级为工作区切换器（列表 / 切换 / 新建 / 重命名，切换后整页刷新重载全部模块数据），所有仓库查询自动按激活工作区隔离
- 新增全量数据导出（蓝图 §1 私有原则）：`GET /api/export` 以 JSON 附件输出全部 26 张表（Date 序列化为 ISO 字符串，文件名含导出日期），设置面板新增「数据备份 · 导出」入口；仓库层 `exportWorkspaceData()` 按 schema 表名遍历全库，集成与合同测试覆盖完整性
- 落地用户可配置项面板（蓝图 §6）：工作台设置弹层升级为完整面板——老师风格全局默认（六选一格）、内容深浅（一句话/详解/带代码）、复习保留率目标（85%/90%）、每日新学量与成长目标（含快捷预设），读写 `/api/settings` 乐观更新失败回滚；深浅指令注入聊天系统提示词，保留率参数化 FSRS 调度器（按档位缓存实例，预览与调度一致），到期队列与今日负荷按「每日新学量 × 今日已引入新卡」过滤新卡并保持摘要一致，今日页展示成长目标徽标
- 落地老师风格系统第一阶段（蓝图 §5）：新增 `workspace_settings` 表（含场景覆盖与可配置项全部列，迁移 0014）与 `GET/PATCH /api/settings`；`lib/ai/teacher-style.ts` 定义六种风格（苏格拉底/讲师/费恩/实战/陪伴/严师）及其系统提示词指令，`/api/chat` 按「会话内临时切换 → 全局默认」解析风格注入系统提示词；聊天工具行新增风格切换器（localStorage 持久、可恢复跟随全局，不写事件流）；`/dev/*` 演示页加生产环境 404 守卫
- 依评估决策方案 A 正式除名实践区：删除 `/api/practice` 路由、`lib/practice/`（挑战库 / 校验器 / 类型）、`tests/practice-validator.test.ts`、db 层 practice 写入函数与对应合同 / 集成用例；`practice_attempts` 表、历史事件与分析聚合保留（活动账本中历史练习标题改用静态标签兜底）
- 同步文档基准：产品设计蓝图与 08-14 计划补记实践区除名及手绘色板推翻（含阶段 13–18 复选框补勾），CLAUDE.md 修正 FSRS（ts-fsrs 6 适配层）与知识图（`lib/knowledge/` + 数据库表）描述并补全测试范围说明，AGENTS.md / README 项目结构更新，移除已完成使命的根目录 `todo.md`
- 完成模块对照蓝图评估（`docs/reports/2026-08-24-模块对照蓝图评估.md`）：对照三份计划文档盘点各模块达标度，给出修改（实践区遗留资产、文档漂移、dev 页守卫）、补充（P1 老师风格系统与可配置项、P2 导出备份与多工作区、P3 遗忘预警与资源推荐）、移除（悬空 practice 代码、todo.md 归档）清单与实践区去留决策项

## 2026-08-15

- 下线实践区页面与客户端 SQL 沙盒：删除 `/practice` 页面、导航入口、sql.js worker 与 wasm 资产并卸载 sql.js 依赖，练习类引导统一改指模拟面试或成长分析活动账本；保留 `/api/practice`、SQL 任务数据与校验纯函数，并同步 CLAUDE.md / AGENTS.md / README 的模块说明
- 参照 StyleKit Hand-drawn Doodle Showcase 对全站完成纸张手绘风格替换：统一纸白/暖纸、墨黑虚线、红/青/黄 marker、胶带、横线纸、硬边错位纸影和轻微不规则落位，移除玻璃、模糊、泛光、氛围渐变及旧 `glass-*` 实现语义；八个可见页面、壳层、弹层、表单、状态组件与知识白板全部迁移，并保留数据图表和长文本的直线可读性
- 补齐手绘交互状态：按钮按压后落入纸影、列表与节点悬停轻抬、弹层和页面纸张落位、会话纸卡直接压叠/悬停抽出/祖先回溯、陌生 Concept marker 高亮与悬浮定义；完成 1440×900、1024×768 全页面截图审查及会话堆叠、术语悬浮、多来源轨道专项截图
- 将会话实体卡堆升级为紧密牌堆交互：父级与分支默认直接叠放并露出 28px 可识别卡沿，悬停或键盘聚焦任一卡沿时整叠拉开、目标层抬升并增强层级阴影，切换时仅使用 transform/opacity 模拟抽卡到前景，并新增展开态截图与动效断言
- 将会话导航重新设计为同尺寸实体卡直接堆叠：父会话向左上退后、子分支向右下展开、当前卡始终置顶；每张露出独立 24px 可点击标题带，前两层直接切换、更多层由第三张卡打开列表，加入回到父级/进入分支的方向动效，并完成 1440、1024、Concept 轨道和三兄弟分支的截图与点击回归
- 按 Next.js 16.3 项目内置文档将自动生成的 `next-env.d.ts` 加入 gitignore 并停止跟踪，避免 `next dev` 与 `next build` 切换类型输出目录时反复污染工作树
- 完成学习闭环升级阶段 18：以“今日 → 对话 → Concept → 消息级语义分支 → SQL 实践 → 主动复习 → 证据化分析”建立最终确定性端到端验收，覆盖 1440×900、1024×768 与 390 基础护栏；新增临时 SQLite 全迁移、quick check、外键和重复迁移健康测试，修复 1024 复习主操作裁切、Concept 追问后轨道遮挡、客户端临时消息来源解析及资源保存错误被弹窗遮挡问题，重写最终设计系统与交付审计，并完成最终截图矩阵和全量质量门禁
- 完成学习闭环升级阶段 17：建立桌面优先的全局可访问性与布局质量门禁，新增跳到主内容、统一焦点环、模态会话选择器焦点返回、Concept 轨道 Escape、强制颜色与减少动态支持；以 axe 扫描 10 个关键路由并检查 24×24 目标尺寸、1024/1440/1600 横向溢出和 CLS，390 仅保留基础路径护栏；同时增加旧 ReviewCard 的 FSRS 读取兼容，修复真实今日页因历史调度数据越界返回 500，并完成 8 张键盘、深色、高对比度、长文本与紧凑桌面截图验收
- 完成学习闭环升级阶段 16：统一 `workspace / concept / source / attempt` 学习上下文深链，新增全局来源路径、返回来源与退出上下文；笔记、资源、对话 Concept、实践、面试、复习和成长分析可围绕同一对象连续跳转，目标对象进入后自动聚焦；为 App Router 的内部工作区补充按完整 URL 保存的滚动位置，浏览器前进/后退可恢复页面、选择和滚动，并完成同一 Cache-Control 概念跨五个模块的 1440/1024 截图旅程验收
- 完成学习闭环升级阶段 15：将累计总量看板改为可行动成长分析投影，以 LearningEvent、PracticeAttempt、InterviewAttempt 与 ReviewLog 区分学习活动、评测、复习和提示依赖；首屏展示今日负荷与单一建议，新增 7/30 天可点击节律图、证据化薄弱技能、真实进步和对象级流水，小于 3 次的比率明确显示“样本不足”，并完成正常、30 天、日期下钻、评测过滤、小样本和新工作区六态桌面截图验收
- 完成学习闭环升级阶段 14：将只读硬编码 SVG 白板升级为数据库驱动的局部知识图，分离知识节点、带证据语义边与画布坐标；基于真实 Concept 和来源共现生成一/二跳图，引入 React Flow 的缩放、平移、fit、MiniMap、搜索与关系过滤，节点轨道展示六类掌握依据和下一步动作，会话图可回到 fork 消息，并完成 1440 五态截图与 1024 基础门禁
- 完成学习闭环升级阶段 13：将资源库重构为收件箱、在读、已完成三队列知识输入箱，引入 URL 规范化、元数据安全提取、工作区去重合并和资源与 Concept 多对多关系；支持搜索、类型/标签筛选、编辑删除、阅读进度及带来源定位的摘录，并让聊天显式选择资料、按来源编号回答且刷新后引用仍可追溯，完成七态桌面截图验收
- 完成学习闭环升级阶段 12：将模拟面试重构为场次、题目和不可变作答版本三层模型，开始前可选岗位、主题、难度、轮数与面试官风格；作答页只保留题目、计时和无评分线索追问，提交后按正确性、结构、证据、表达四维展示原文证据、参考答案和下一步策略；持久化 advance/stay/downgrade 驱动下一题难度，支持同题重答差值与失败题跳转 Concept，并完成六态桌面截图和三题自适应验收
- 完成学习闭环升级阶段 11：将 SQL 沙盒改为三题任务驱动工作台，按题目、Schema、编辑器和验证结果组织桌面界面；SQL 在带 1.5 秒超时和 100 行上限的 Web Worker 中逐次重建种子库，以结果集、列序、行内容、修改行数及最终状态验收；新增分级提示、解法、重置、下一题和 PracticeAttempt 技能证据，并完成查询成功、语法错误、提示及副作用五态截图验收
- 完成学习闭环升级阶段 10：引入 `ts-fsrs` 的 FSRS 6 正式实现并封装可替换调度 adapter，新增完整 ReviewCard、不可变 ReviewLog 和追加式撤销记录；复习改为输入/口头主动回忆后揭示答案，四档评级显示服务端同源预计间隔，支持撤销、键盘、来源与困难卡，并完成旧掌握度迁移和五态桌面截图验收
- 完成学习闭环升级阶段 9：笔记区分不可变 AI 快照与用户编辑版本，新增来源消息范围、标签检索、目录、版本记录和三项明确学习动作；代码块支持语言标签、横向滚动与复制，来源删除后保留失效提示，并完成阅读、编辑、版本、失效来源和空状态桌面截图验收
- 完成学习闭环升级阶段 8：将今日学习从静态三项改为真实数据行动投影，按最近会话与事件、到期复习、薄弱概念、未完成资源和最近笔记生成单一主操作；支持本地“今天稍后处理”、首次起步和全部处理状态，并完成三态桌面截图验收
- 完成学习闭环升级阶段 7：将术语演进为带规范名、别名、示例和置信度的 Concept，新增跨消息/笔记/资源来源 Mention；术语改为键盘可达触发器，聊天右侧接入多来源上下文轨道和继续追问、笔记、练习、复习动作，并完成键盘、加载、多来源截图验收
- 完成学习闭环升级阶段 6：会话新增根会话与消息锚点，分支创建只提交锚点并由服务端组装祖先共享前缀；术语和消息均可直接派生带首问的语义分支，补充卡片进场动效、1024 紧凑路径、上下文继承集成测试与分支前中后截图
- 完成学习闭环升级阶段 5：会话改为最近活动优先并按首问本地命名，新增搜索、重命名、置顶、归档恢复与确认删除；历史消息返回稳定状态和术语来源，输入区补齐停止、重新生成和继续回答，并通过桌面截图与完整会话生命周期测试
- 完成学习闭环升级阶段 4：为全部 Route Handler 建立 zod 运行时边界和统一错误结构，扩展版本化 LearningEvent，贯通会话、消息、术语、笔记、面试、复习、资源的幂等键与状态/事件同事务写入；新增 SQLite 迁移、临时库回滚测试和 API 合约测试
- 完成学习闭环升级阶段 3：建立统一客户端请求错误与超时结构，补充紧凑型局部反馈；聊天加入中止、请求序号和会话校验，资源、笔记、面试、复习改为失败可重试且保留输入；通过 400/500/超时单测、5 个桌面 Playwright 场景和 4 张状态截图验收

## 2026-08-14

- 完成学习闭环升级阶段 2：重构为桌面左侧分区导航、顶部上下文条、平板图标栏与基础移动底栏，新增 `/today` 行动入口、页面搜索、主题设置和上下文轨道能力
- 完成学习闭环升级阶段 1：收敛浅/深主题语义令牌，引入 Lucide 与按需 Radix primitives，建立按钮、字段、分段控制、Toast、Skeleton、空/错状态及开发视觉校验页
- 完成学习闭环升级阶段 0：引入 Playwright 四视口截图工具链，生成五个核心页面的 20 张视觉基线与布局审计，并形成桌面优先的现状审查报告
- 新增「学习闭环与前端交互升级」细粒度实施计划：明确学习工作台布局、上下文轨道、消息级分支、19 个实施阶段及桌面/移动端截图验收流程
- 同步实施计划中的 Next.js 版本（15 → 16.3.0）
- 补充 `README.md`（快速开始 + 常用命令 + 文档索引）
- 完善 `CLAUDE.md`（命令 + 架构大图 + 现状说明）
- 新增 `todo.md`（核心链路开发计划：阶段 0 + 阶段 1 细致任务清单）
- 完成阶段 0：DeepSeek provider、流式对话 API（`/api/chat`）、术语标注两段式（`/api/terms` + `[[术语]]` 内联）、术语高亮与悬停弹窗
- 完成阶段 1：Drizzle + better-sqlite3 六表数据模型、会话 CRUD 与消息持久化、学习事件流、会话树侧边栏
- 收尾：抽取会话树 / 术语解析两个纯函数到 `lib/`，引入 Vitest 并写单测（10 用例通过）
- 修正 DeepSeek 模型 ID：`deepseek-chat`/`deepseek-reasoner` → `deepseek-v4-flash`/`deepseek-v4-pro`（旧 ID 已于 2026-07-24 下线）
- 定位 401 根因：环境变量 `DEEPSEEK_API_KEY` 覆盖了 `.env` 中的有效 key（Next.js 不覆盖已存在的环境变量）
- 新增「学习笔记」模块：`/api/notes` + `/notes` 页，结构化总结（核心概念/术语/代码示例/未懂点）+ 导出 Markdown
- 新增「模拟面试」模块：`/api/interview` + `/interview` 页，出题 + 分层判分（advance/stay/downgrade）
- 重构 DB 连接为懒加载单例，避免 `next build` 多 worker 并发初始化 SQLite 造成 SQLITE_BUSY
- 新增「隐性巩固」模块：简化版 FSRS 调度器 + `/api/review` + `/review` 复习卡片页（术语自动入队，答后按评级排期）
- 新增「成长分析」模块：`/analytics` 仪表盘（术语掌握度分布、面试正确率、学习行为分布、最近活动）
- 新增「白板」模块：`lib/tree-layout.ts` 树布局引擎 + `TreeGraph` SVG 渲染器，`/whiteboard` 页展示会话关系图与个人成长地图（内置后端能力树 + 掌握度热力）
- 新增「资源库」模块：`resources` 表 + `/api/resources`（增/查/改状态）+ `/resources` 页（添加资源、按术语关联、想读/在读/已读状态）
- 新增「实践区」模块：sql.js（WASM）SQL 沙盒 + `/practice` 页（内置示例表、运行查询、code_run 事件落库）
- 更新 `CLAUDE.md`：同步八大模块落地现状、Vitest / Drizzle 常用命令与架构大图（旧版仍停留在骨架阶段描述）
- 全量文档同步：`README.md`（技术栈表标注未安装项、补 test / drizzle-kit 命令、项目结构改实际）、`AGENTS.md`（技术栈表 + 项目结构同步现状）、`todo.md`（补状态说明、401 根因更正、测试计数 4 文件 18 用例实测通过）、`docs/plans/` 两份文档头部状态行（蓝图标记愿景基线、实施计划标记已执行完成并列出选型出入）；`DESIGN.md` 与 `globals.css` 逐项核对一致，未改动
- 前端 UI/交互整体重构（纯前端，后端 API / DB 不动）：
- 主题系统：默认浅色 + 深色可切换——globals.css 重写为 `:root`(浅)/`.dark`(深) 双套令牌（新增 `card-foreground` / `*-foreground` / `surface` / `state-untouched` 语义令牌），`@custom-variant dark` 类策略，layout 防闪内联脚本 + `localStorage['mentor-theme']` 记忆
- 令牌语义迁移：9 文件 54 处 `text-background` → `text-card-foreground` 等，彩色填充统一配对 `*-foreground`，分区块底统一 `bg-surface`
- AppShell 全局壳：顶栏「学习|测验」大胶囊 + 组内子标签 + 右侧全局工具（分析/白板）+ ThemeToggle；跨组切换记忆上次页；新增 `lib/nav.ts` 导航配置与 `PageShell` 统一页头，8 个模块页去复制粘贴头、去星型导航
- SessionDeck 会话卡片堆替代侧边栏：当前会话大卡片 + 祖先链左侧竖条堆（点回跳，>3 折叠）+ 分支子会话右上角卡片扇（点切入，>3 折叠）+ SessionPicker 全部会话弹层树；Chat.tsx 拆分为 6 个组件（状态容器/舞台/大卡/祖先堆/分支扇/选择器），流式与术语逻辑原样保留
- 补上模型切换 UI：卡片标题栏「闪电/深思」小胶囊（接通后端已有 `model` 参数，localStorage 记忆）
- 白板主题化：TreeGraph 连线 `stroke-border`、节点走 `var(--令牌)` style 注入（SVG attribute 不支持 var()）；stateToStyle 未接触态/新发现态改令牌化
- 文档：DESIGN.md 全量重写（双主题令牌表 + 导航 + 卡片堆规范），CLAUDE.md 架构大图同步
- 验证：`npm test` 18/18、`npm run lint` 0 错、`npm run build` 通过、dev 全 8 路由 200

## 2026-08-13

- 初始化 Next.js 16 项目骨架（App Router + TypeScript + Tailwind v4）
- 落地 card-stack 设计令牌（深紫黑背景 + 紫/青/粉/黄点缀）
- 编写产品设计蓝图（八大模块）与项目初期实施计划（`docs/plans/`）
- 配置 DeepSeek API 环境变量（`.env`，已 gitignore）
- 补充 AGENTS.md / DESIGN.md / CHANGES.md 三份项目文档
