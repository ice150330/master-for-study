# DESIGN.md · Mentor 手绘学习工作台设计系统

> 最终基线：Hand-drawn Doodle、桌面优先、对象可追溯。界面像一张持续使用的学习桌纸面，而不是营销首页或装饰性卡片墙。

## 1. 产品结构

应用以“下一步行动”而不是八个同权模块组织信息：

```text
今日学习
  → 对话学习 → Concept → 语义分支
  → 学习笔记 / 资源库
  → 模拟面试 / 主动复习
  → 成长分析 / 知识白板
```

Concept 是跨模块单源知识对象。消息、笔记、资源是来源证据；PracticeAttempt、InterviewAttempt、ReviewLog 是掌握证据；LearningEvent 是可追踪行为。页面跳转通过 `workspace / concept / source / attempt` URL 参数保留同一学习上下文，不依赖隐式全局状态。

## 2. 桌面工作台

主验收面为 1440×900 和 1024×768，1600 用于宽屏与长文本校准。390 只保留基本导航和关键内容，不为手机端复制复杂桌面工具。

应用没有全局顶栏（2026-08-25 起移除）：桌面布局 = 全高侧边栏 + 工作区列。侧边栏自上而下是徽标、四区导航与底部工具区（工作区切换器 + 搜索 / 设置 / 主题）；设置是大尺寸弹窗（38rem，标题 + 分区滚动内容），桌面与移动共用。工作区列 = 页面滚动区 + 常驻页底的学习上下文条（h-8，工作区 → 来源 → 概念 → 作答 → 当前位置，无上下文时收成工作区与当前位置）。

| 宽度 | 应用壳 | 复杂工作区 |
|---|---|---|
| ≥1180 | 196px 文字侧栏（底部工具区） | 对话/资源/白板使用主区 + 右轨道或多栏布局 |
| 768–1179 | 56px 图标侧栏（工具收成图标） | 保留完整桌面能力；Concept 改为便利贴弹窗，复杂工作区压缩密度 |
| <768 | 56px 底部区域导航（四区 + 工具格） | 仅保证今日、对话、复习等基础路径可达且页面不横向溢出 |

侧栏分为今日、学习、测验、洞察四区。模块级操作进入各自页头或工作区工具栏。

## 3. 风格评估与采用边界

视觉基准参考 [StyleKit Hand-drawn Doodle Showcase](https://www.stylekit.top/styles/hand-drawn-doodle/showcase)。纸张、墨线、虚线轮廓、胶带、图钉、马克笔划线和轻微错位与学习产品的草稿、记忆和推演语义一致，适合本项目；但原样把旋转、粗线和装饰铺满数据表、长文或图谱会降低扫描效率。

因此采用“结构化手绘工作台”：全局材料、控件、弹层、状态和动效完整使用手绘语法；正文、表格、图表和画布保持稳定直线几何。**全局字体为站酷快乐体（ZCOOL KuaiLe，免费可商用，圆润手写风、近似奶酪体）**，按 unicode-range 分片按需加载，直接挂在 body 上覆盖全部界面文字；代码块仍走等宽字体。图标即可表意的按钮缩减为纯图标 + Tooltip 悬停显示功能（发送 / 停止、引用资源、新话题等）；文字承载语义的动作（回复操作行、对话框确认）保留文字。禁止高斯模糊、透明玻璃、霓虹泛光、氛围渐变和大圆角。单个视区只允许少量胶带或 marker 作为定位标记，装饰不能成为信息层。

## 4. 颜色与层级

令牌定义在 `src/app/globals.css`，组件只使用语义类，不直接复制颜色值。全局主题共四种内置纸张（`<html data-theme>`，纸白为默认无属性）：**纸白**（默认冷白）、**暖纸**（米黄护眼）、**青蓝**（冷调图纸，适合读代码与图表）、**夜墨**（唯一深色——墨绿底、米色墨、marker 提亮，`dark:` 变体只作用于它）。切换入口：侧边栏调色板与设置弹窗；旧值 `light` / `dark` 自动迁移为纸白 / 暖纸。

| 令牌 | 纸白 | 暖纸 | 用途 |
|---|---|---|---|
| `background` | `#fffef5` | `#f7f2df` | 横线笔记纸背景 |
| `foreground` | `#2c2c2c` | `#2c2c2c` | 墨黑标题、正文和主要边线 |
| `surface` | `#f3f7ef` | `#edf3e6` | 列表、轨道和次级纸片 |
| `card` | `#fffef5` | `#fffaf0` | 主纸张和对象工作区 |
| `card-soft` | `#faf9ed` | `#f5efdd` | 输入、气泡、代码与内嵌区 |
| `primary` | `#b43b46` | 同纸白 | 可读的红色路径、校对文字和危险动作 |
| `accent` | `#21736e` | 同纸白 | 可读的 Concept、来源关联和完成文字 |
| `highlight` | `#ffd93d` | 同纸白 | marker 高亮、选中和重点词 |
| `marker-red/teal/yellow` | `#ff6b6b / #4ecdc4 / #ffd93d` | 同纸白 | 非文字的手绘划线、纸影和强调块 |
| `muted` | `#656b65` | `#666a63` | 次级说明，仍保持可读 |
| `border` | 38% 墨黑 | 同纸白 | 虚线分隔与控件轮廓 |

规则：三支 marker 只承担红色路径、青色关联、黄色强调；彩色不承载唯一含义。阴影是无模糊的硬边错位纸影，不能使用发光。夜墨主题下 marker 与阴影令牌整体提亮，组件不写死颜色。

## 5. 形状与密度

- 页面分区使用虚线、留白和纸色，不把每个区块做成悬浮卡片。
- 重复对象、模态框和真正工具容器使用 0–2px 圆角纸片；卡片不嵌套卡片。
- 图标按钮稳定为 32–40px；所有非内联指针目标至少 24×24 CSS 像素。
- 面板标题使用 14–18px，页面标题 24–28px；不按视口缩放字号，字距保持 0。
- 数据密集页优先表格、分栏、轨道和紧凑列表，禁止卡片套卡片。
- **全局手绘滚动条**（设计系统组件，globals.css 通用选择器）：滑块为半透明墨色填充 + 墨色虚线描边的手绘小框，轨道透明露出纸张底色，悬停 / 按下换红色；令牌驱动四主题自适应，Firefox 退化为同色细条。
- 标题最多轻微旋转，正文、输入框、图表坐标和表格永不旋转；胶带与折角不可遮挡文本。

## 6. 核心交互模式

### 对话与语义分支

当前会话是唯一主舞台，主卡占满可用高度，**不再为导航预留任何尺寸**。会话血缘通过两层机制表达（2026-08-25 起为「胶带纸签 + 树抽屉」，当日稍晚由边缘纸签改为骑缝胶带）：

**胶带纸签（上下文快速跳转）**：父级链是骑缝贴在主卡**上缘**、从左上角向右排成一行黄色半透明胶带（最近父级最靠左，标签为 父 / 祖父 / 上游N + 截断标题）；子分支是贴在**下缘右下角**的青色胶带**真实重叠堆叠**（`.session-tape-stack`）：收起时逐条向左叠压只露图标与「分支N」层号（31px 步长、轻微上错位），**鼠标悬停 / 键盘聚焦整叠扇形展开**——胶带横向散开并淡入完整标题（220ms，仅 margin / max-width / opacity）。胶带为**撕裂不规则端**（共享 `--tape-torn` clip-path：两端锯齿 + 微波动边缘，无边框；键盘焦点用四向 drop-shadow 拼出跟随形状的描边）。卡片顶部黄条、概念便利贴青条同款。胶带一半在卡外（落进页面上下 16px 留白）、一半压在卡边框上，**收起态不遮挡卡内任何内容**。每侧最多 3 条 + 「+N」溢出胶带（并入堆叠）。桌面与移动同一套胶带。

**会话树气泡（全局导航）**：从卡片头部树形按钮**向下弹出**的气泡面板（Radix Popover 锚定触发器，顶部虚线小尾巴指回按钮；外点 / Escape 关闭、焦点语义继承）。**鼠标悬停 180ms 即开、移开 240ms 即关**（移进面板停留不关、快扫不误触；触摸设备保持点击开关）。内容为完整会话树（`buildSessionTree`，与白板同源）：搜索、当前路径高亮、置顶标记、相对时间、归档恢复与新话题（头部图标钮）。入口有二：同一触发按钮与「+N」胶带溢出（共用受控 open）。

**卡片头部（单行状态栏）与输入区**：头部是单行信息——深度徽标 `D0` · 标题 · 血缘与消息计数（窄屏收敛）+ 工具组（**工作区切换器**（会话界面独有）、会话树、新话题、会话操作菜单）。闪电 / 深思是**单按钮双态切换**；输入框是外框容器（边框与焦点态挂在框上，focus-within 抬升 + 青色纸影），老师风格、模型切换、引用资源三个图标钮**内嵌框内右侧**，透明 textarea 随内容增高；发送钮在框外、边框加粗 3px。AI 消息组紧跟提问上移（头像与提问行大致齐平，负边距仅在上一条为用户消息时生效）。

**回复操作行与 Markdown**：每条完整 AI 回复的气泡**正上方靠左渲染 AI 老师头像**——`MentorAvatar` 手绘小机器人（学士帽 + 天线 + 圆眼腮红，内联 SVG 全走主题令牌，四主题自适应，硬边纸影 0 模糊 drop-shadow）；气泡左上角有虚线折角尾巴指向头像（同底色的 rotate 45° 方块，`border-l/t` 虚线，视觉为对话气泡开口）。气泡下方常驻小型操作行——**重新生成**（仅最后一条可原位重写，更早的回答置灰并提示改用分支）、**详细综述**（从该回答派生分支，生成一句话结论 + 要点展开 + 例子与误区 + 追问方向的结构化综述）、**从这里分支**；三个动作均为图标钮 + Tooltip，用户消息不带头像与尾巴、不再提供分支入口。助手回复走全局 Markdown 渲染（react-markdown + remark-gfm，`.md-chat` 手绘主题块）：标题带 marker 划线、无序列表用铅笔符 ✎、粗体带 marker 笔触、行内代码虚线小框、代码块横向滚动、表格虚线网格、链接波浪下划线；`[[术语]]` 在文本节点内解析为高亮 Term（行内代码保持字面），用户消息保持纯文本不误格式化。

**会话树路径状态条（页底常驻）**：目录式显示 根 → … → 当前会话 的完整血缘路径（`session-trail` store 连接 Chat 与壳层），上游各段可点击直接切换（其他页面跳转 `/?session=`），当前段 marker 高亮，尾部显示分支数；非会话页回退为工作区与学习上下文面包屑。图标即可表意的按钮一律为纯图标钮 + Tooltip 悬停显示功能，悬停样式统一走 IconButton（轻抬 1px + 硬边青色纸影 + 高亮底）；文字承载语义的对话框确认与起步问题保留文字。

主卡的 `D0 / D1 / …` 表示从根会话开始的真实深度，血缘标题栏显示「承自 <父标题>」与分支计数。用户可从具体消息或 Concept 继续追问，服务端按 fork 消息组装共享前缀；创建分支后 Concept 弹窗自动关闭，让新问题和回答立即可见。整张当前卡不支持拖拽，因为消息滚动、术语触发器和输入框必须保持稳定的原生交互。

流式请求可停止；切会话会中止旧请求，旧响应不得覆盖新会话。

### Concept 便利贴

点击未知知识点弹出**便利贴弹窗**（替代原右侧轨道与底部覆盖工作面）：居中 Dialog（30rem 上限），黄色便签纸底 + 顶部青色胶带装饰，内容为定义、置信度、来源锚点、相关笔记 / 资源与「继续追问、创建笔记、模拟测验、加入复习」。Radix 焦点语义（首焦点、Escape、焦点返回）由 Dialog 提供；来源跳转继续写入 URL 上下文。

### 学习任务

- 今日：按继续会话、到期复习、薄弱概念、在读资源、最近笔记排序，只给真实证据。
- 笔记：左列表 + 可编辑正文 + 目录/动作轨道，AI 快照不可变，用户版本可回溯。
- 资源：收件箱/在读/完成队列，URL 先取元数据，再关联多个 Concept、标签、摘录与进度。
- 面试：设置 → 问题/计时 → 四维 rubric → 下一题策略；难度由上次结果驱动。
- 复习：主动输入或口头回忆 → 查看答案 → 四档评级/预计间隔 → 撤销；1024 首屏必须看见主操作。
- 分析：负荷与单一建议优先，其次节律、薄弱技能和证据流水；样本不足必须直说。
- 白板：局部一/二跳图，支持缩放、平移、关系筛选、节点轨道与回到来源。

## 7. 请求与反馈

加载、空、错误、成功、超时和重试使用统一 primitives。错误必须靠近发起操作的位置：表单错误留在对话框内，列表错误留在列表上方；失败不清空输入。Toast 只确认短暂结果，不承载唯一错误说明；相同通知合并，避免遮挡工作区。

## 8. 动效与可访问性

- 全局 `:focus-visible` 使用 3px `ring` + 1px 背景分隔，跳到主内容是首个 Tab 目标。
- Dialog/Popover 建立首焦点、Escape、关闭按钮与焦点返回；图标按钮必须有可访问名称和 Tooltip。
- `prefers-reduced-motion: reduce` 下动画与过渡接近 0；主题切换期间禁用颜色过渡，避免低对比中间帧。
- 页面与弹层以 2–8px 的纸张落位进入；按钮按下时位移到硬边阴影，悬停只抬升 1px。禁止无限漂浮、发光呼吸和无目的旋转。
- 强制颜色模式使用系统色绘制焦点、当前项和选中态。
- 长 URL、消息和正文允许断行；代码和二维画布在自身区域内滚动、缩放，不推动页面横向溢出。

自动门禁位于 `tests/e2e/accessibility-layout.spec.ts`：9 个关键路由进行 axe 严重级扫描，检查目标尺寸、1024–1600 页面溢出、CLS、暖纸、强制颜色、减少动态和 390 基础路径。自动扫描不等于完整 WCAG 合规，发布前仍需读屏人工验证。

## 9. 截图与验收

每个阶段使用固定 fixture 或网络 mock，在 `data/ui-captures/<phase>/` 生成截图并人工查看。Hand-drawn Doodle 基线位于 `data/ui-captures/35-hand-drawn-doodle-audit/`：1440 与 1024 均覆盖八个可见页面，另含会话堆叠展开、祖先回溯、Concept 悬浮、键盘焦点、加载和多来源轨道状态。

功能完成必须同时满足：Vitest、ESLint、TypeScript、Next production build、SQLite quick/foreign-key 检查，以及 Playwright 对应流程通过。DOM 断言和构建成功不能替代截图审查。
