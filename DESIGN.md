# DESIGN.md · Mentor 学习工作台设计系统

> 最终基线：低噪声、桌面优先、对象可追溯。界面服务于连续学习与反复操作，不采用营销首页、装饰性卡片墙或大面积氛围图形。

## 1. 产品结构

应用以“下一步行动”而不是八个同权模块组织信息：

```text
今日学习
  → 对话学习 → Concept → 语义分支
  → 学习笔记 / 资源库
  → SQL 实践 / 模拟面试
  → 主动复习
  → 成长分析 / 知识白板
```

Concept 是跨模块单源知识对象。消息、笔记、资源是来源证据；PracticeAttempt、InterviewAttempt、ReviewLog 是掌握证据；LearningEvent 是可追踪行为。页面跳转通过 `workspace / concept / source / attempt` URL 参数保留同一学习上下文，不依赖隐式全局状态。

## 2. 桌面工作台

主验收面为 1440×900 和 1024×768，1600 用于宽屏与长文本校准。390 只保留基本导航和关键内容，不为手机端复制复杂桌面工具。

| 宽度 | 应用壳 | 复杂工作区 |
|---|---|---|
| ≥1180 | 224px 文字侧栏 + 64px 顶栏 | 对话/资源/实践/白板使用主区 + 右轨道或多栏布局 |
| 768–1179 | 64px 图标侧栏 + 64px 顶栏 | 保留完整桌面能力；Concept 改为底部覆盖轨道，复习与实践压缩密度 |
| <768 | 56px 顶栏 + 64px 底部区域导航 | 仅保证今日、对话、复习等基础路径可达且页面不横向溢出 |

侧栏分为今日、学习、测验、洞察四区。全局顶栏只保留当前页面、工作区、搜索、设置和主题；模块级操作进入各自页头或工作区工具栏。

## 3. 颜色与层级

令牌定义在 `src/app/globals.css`，组件只使用语义类，不直接复制颜色值。

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `background` | `#f6f7f9` | `#18191c` | 页面和工作台底色 |
| `foreground` | `#1b1d22` | `#f3f3f2` | 页面级标题与正文 |
| `surface` | `#eef0f3` | `#202226` | 分区、列表、辅助底色 |
| `card` | `#ffffff` | `#25272b` | 对象面板和工具表面 |
| `card-soft` | `#f3f4f6` | `#2c2f34` | 输入、气泡、代码与内嵌区 |
| `primary` | `#6558d9` | `#948cf0` | 当前路径与唯一主操作 |
| `accent` | `#066d65` | `#45bdb1` | Concept、成功与来源关联 |
| `warning` | `#b96d0b` | `#e0a452` | 需要注意但可继续的状态 |
| `danger` | `#a93650` | `#e27c91` | 失败与不可逆动作 |
| `muted` | `#656a74` | `#a9adb6` | 次级说明，仍需保持可读 |
| `border` | `#d9dce3` | `#3b3e45` | 分隔与控件轮廓 |

规则：彩色填充必须搭配对应 `*-foreground`；黄色不用作小字号正文；一屏只允许一个视觉主按钮；紫色表达路径/动作，青色表达知识关联/成功，危险色不承担普通强调。

## 4. 形状与密度

- 页面分区使用边线、留白和底色，不把每个区块做成悬浮卡片。
- 重复对象、模态框和真正工具容器可用卡片，圆角不超过 8px；会话主舞台保留较柔和外轮廓以表达独立工作面。
- 图标按钮稳定为 32–40px；所有非内联指针目标至少 24×24 CSS 像素。
- 面板标题使用 14–18px，页面标题 24–28px；不按视口缩放字号，字距保持 0。
- 数据密集页优先表格、分栏、轨道和紧凑列表，禁止卡片套卡片。

## 5. 核心交互模式

### 对话与语义分支

当前会话是主舞台；祖先以左侧竖条回溯，子分支以分支入口切换。用户可从具体消息或 Concept 继续追问，服务端按 fork 消息组装共享前缀。创建分支后 Concept 轨道自动退场，让新问题和回答立即可见。

会话列表是模态 Popover：搜索框首焦点，内部有明确关闭按钮，Escape 关闭并恢复触发器焦点。流式请求可停止；切会话会中止旧请求，旧响应不得覆盖新会话。

### Concept 轨道

1440 下为右侧持久轨道，1024 下为底部覆盖工作面。轨道包含定义、置信度、来源、关联笔记/资源与“继续追问、创建笔记、开始练习、加入复习”。打开轨道不离开当前任务；进入新动作后来源继续写入 URL。

### 学习任务

- 今日：按继续会话、到期复习、薄弱点实践、在读资源、最近笔记排序，只给真实证据。
- 笔记：左列表 + 可编辑正文 + 目录/动作轨道，AI 快照不可变，用户版本可回溯。
- 资源：收件箱/在读/完成队列，URL 先取元数据，再关联多个 Concept、标签、摘录与进度。
- 实践：任务 → Schema → 编辑器 → 验证结果；结果而非 SQL 字符串决定通过。
- 面试：设置 → 问题/计时 → 四维 rubric → 下一题策略；难度由上次结果驱动。
- 复习：主动输入或口头回忆 → 查看答案 → 四档评级/预计间隔 → 撤销；1024 首屏必须看见主操作。
- 分析：负荷与单一建议优先，其次节律、薄弱技能和证据流水；样本不足必须直说。
- 白板：局部一/二跳图，支持缩放、平移、关系筛选、节点轨道与回到来源。

## 6. 请求与反馈

加载、空、错误、成功、超时和重试使用统一 primitives。错误必须靠近发起操作的位置：表单错误留在对话框内，列表错误留在列表上方；失败不清空输入。Toast 只确认短暂结果，不承载唯一错误说明；相同通知合并，避免遮挡工作区。

## 7. 动效与可访问性

- 全局 `:focus-visible` 使用 3px `ring` + 1px 背景分隔，跳到主内容是首个 Tab 目标。
- Dialog/Popover 建立首焦点、Escape、关闭按钮与焦点返回；图标按钮必须有可访问名称和 Tooltip。
- `prefers-reduced-motion: reduce` 下动画与过渡接近 0；主题切换期间禁用颜色过渡，避免低对比中间帧。
- 强制颜色模式使用系统色绘制焦点、当前项和选中态。
- 长 URL、消息和正文允许断行；代码和二维画布在自身区域内滚动、缩放，不推动页面横向溢出。

自动门禁位于 `tests/e2e/accessibility-layout.spec.ts`：10 个关键路由进行 axe 严重级扫描，检查目标尺寸、1024–1600 页面溢出、CLS、深色、强制颜色、减少动态和 390 基础路径。自动扫描不等于完整 WCAG 合规，发布前仍需读屏人工验证。

## 8. 截图与验收

每个阶段使用固定 fixture 或网络 mock，在 `data/ui-captures/<phase>/` 生成截图并人工查看。最终矩阵位于 `data/ui-captures/final/`：桌面与 1024 均覆盖今日、聊天、Concept、语义分支、实践、复习、分析；390 只覆盖今日和复习底线。

功能完成必须同时满足：Vitest、ESLint、TypeScript、Next production build、SQLite quick/foreign-key 检查，以及 Playwright 对应流程通过。DOM 断言和构建成功不能替代截图审查。
